# My silly little workspace container

## To-do:
- Should I pass in the .ssh folder of the host machine as well? Probably, it's not prod and I don't have a vault.
- Uhhhh looks like the container's struggling to handle icons. Need to figure that out.
- Would be nice if the script pulled the repo tag and apparended it image tag
- Is podman run the best way to attach to a running container with a tty?
